int main() {
    decl
        int x, y, a, b;
    enddecl
    begin
        //y = exposcall("Write", -2, "Filename");
        //y = exposcall("Read", -1, b);
        //y = exposcall("Write", -2, b);
        y = exposcall("Exec", "odd.xsm");
        // y = exposcall("Write", -2, b);
        return 0;
    end
}
